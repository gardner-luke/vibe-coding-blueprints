# Databricks Configuration
# Fill these values BEFORE running setup_data.py
# The setup_data.py script will read these values and deploy the bundle automatically

catalog: "CATALOG_NAME"
schema: "SCHEMA_NAME"
warehouse_id: "WAREHOUSE_ID"

# Tables (created by the bundle)
tables:
  metadata: "turbines_metadata"
  telemetry: "turbine_telemetry"
